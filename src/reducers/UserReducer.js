let defaultState = {
  isFetching: false,
  isLoggedIn: null,
  profile   : null
};

export function user (state = defaultState, action) {
  switch (action.type) {

    default:
      return state;
  }
}